OCAMLINCLUDES = ../common ../data
OCAML_LIBS = ../common/common ../data/data

USE_OCAMLFIND = true
OCAMLFLAGS = -warn-error A -thread
OCAMLPACKS[] =
  core

OCamlLibrary(horn_solver, solver)

.PHONY: clean
clean:
  rm -f *.cmo *.cmx *.cmi *.cmxa *.a *.omc *.o